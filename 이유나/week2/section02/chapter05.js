// 원시 타입은 값 자체로서 저장, 복사 (메모리 공간에 있는 데이터 값이 변하지 않음 -> 불변값)
// 객체는 참조값을 통해 변수에 저장, 복사 (메모리 값 수정O, 가변값)

// 객체 타입 주의사항 1. 의도치 않게 값이 수정될 수 있다.
// 주의사항 2. 객체 간의 비교는 기본적으로 참조값을 기준으로 이루어진다.
// 주의사항 3. 배열과 함수도 사실 객체이다.
